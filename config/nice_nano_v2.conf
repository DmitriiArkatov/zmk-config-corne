# 32kHz clock from internal RC oscillator
# Needed for ProMicro52840 / nice!nano clones without a 32.768 kHz crystal.
# Without this the firmware hangs at boot (no USB, no Bluetooth).
CONFIG_CLOCK_CONTROL_NRF_K32SRC_RC=y
